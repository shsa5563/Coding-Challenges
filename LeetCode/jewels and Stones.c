class Solution {
public:
    int numJewelsInStones(string J, string S) {
        unordered_set<char, int> temp; 
        for(int i=0; i<S.size() ; i++) ++temp[S[i]; 
        for( auto i:J)
        {
            if()
        }
        
    }
};
